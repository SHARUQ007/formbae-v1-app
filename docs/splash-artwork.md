# Splash artwork

Restored the earlier three-person gym splash from the version before the mosaic redesign.

Asset: `src/assets/editorial/startup-people-hero-v3.jpg` (160,503 bytes).

The React Native splash uses the original full-screen photograph, gradient overlay, FormBae logo, rotating gym loading messages, motivational text, and animated progress bar. The iOS native launch screen uses the original brand layout before handing off to the photo splash.

Startup routing and preload deadlines are unchanged.
